import React from "react";

const Loading = () => {
  return (
    <div className="w-screen h-screen flex justify-center place-items-center">
      <p className="btn btn-xl loading btn-ghost"></p>
    </div>
  );
};

export default Loading;
